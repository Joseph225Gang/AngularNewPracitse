import { AppHelloWorldDirective } from './app-hello-world.directive';

describe('AppHelloWorldDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHelloWorldDirective();
    expect(directive).toBeTruthy();
  });
});
